name: hello

on: 
- push

jobs:
  echo:
  
    runs-on: ubuntu-latest
    
    steps:
    - id: check out repository
      uses: actions/checkout@v2
      
    - id: say hello
      run: echo hello
      
    - id: multiline script
      run: |
        echo Greeting
        echo good morning
